server:
  port: 7070


spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/noetipo/DAD-2024-1-G2.git # ruta del repositorio git
          searchPaths: config-data
          #username:
          #password:
          default-label: main




  security:
    user:
      name: root
      password: 123456
